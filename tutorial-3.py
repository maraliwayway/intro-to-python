# tutorial-3.py

# 0's are the default game map
# Square braces for lists so we can change the elements
game = [[0, 0, 0],
        [0, 0, 0],
        [0, 0, 0],]

# Proof it's a list
print(type(game))

# Display
for row in game:
    print(row)